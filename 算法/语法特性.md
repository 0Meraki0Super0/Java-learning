
### 语法特性
```java
1.数组
int[] res=new int[]{1,7}// 初始化int[]并赋值

int l = Arrays.stream(weights).max().orElse(0);
int r=Arrays.stream(weights).sum();

Arrays.asList(1,2,3)// 将数组转换为列表。List<Integer>--自动装箱
Arrays.asList(new int[]{1, 2, 3})//注意，基本数据类型的数组会被转为单个元素 List<int[]>

2.集合
List<Integer> list = new ArrayList<>()--可传入int(容量)、列表对象

list.toArray(new int[0][0]);//传入new int[0][0]告诉要返回的数组类型，空数组即可

Deque q=new LinkedList() //双端队列，可peekLast,pollLast()

for(int temp: map.values())
for(Map.Entry<a,b> entry:map.entrySet()){entry.getValue() entry.getKey()}








3.字符串
for (char c : s.toCharArray())// String s
String s= new String(char[])// 可将字符数组转为字符串
s.substring(a,b)//[a,b)
sb.toString()//stringbuilder->string








```

